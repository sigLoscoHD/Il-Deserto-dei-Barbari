<---- SOLUZIONE LIVELLO 6 --->
distanceShield = Math.sqrt(Math.pow(x, 2) + Math.pow(y, 2));
                
if (distanceShield < radiusShield )
    return true;            				               
else
    return false;
-------------------------------------------------------------------------------